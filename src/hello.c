#include <stdio.h>
#include <stdlib.h>

int main(){
	char name[100];
    	printf("Enter your name: "); //asks for user input
    	scanf("%99s", name); //gets user input
      printf("Hi, %s!\n", name); //prints Hello and the name
		  printf("Goodbye, %s\n", user); //prints a goodbye message
	return 0;
}

